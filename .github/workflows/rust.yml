name: Rust

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Checkout Suricata
      uses: actions/checkout@v2
      with:
        repository: dbcfd/suricata
        path: suricata
        ref: sensicata
    - name: Checkout libhtp
      uses: actions/checkout@v2
      with:
        repository: OISF/libhtp
        path: suricata/libhtp
    - name: Install Required libraries
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          libpcap-dev \
          libpcre3 \
          libpcre3-dev \
          libmagic-dev \
          libyaml-dev \
          libjansson-dev \
          zlib1g-dev
        cargo install --force cbindgen
    - name: Build Suricata
      working-directory: suricata
      run: autoreconf -i && ./configure --with-unix-socket && sudo make install && sudo make install-conf
    - name: Format
      run: cargo fmt --all -- --check
    - name: Build
      run: cargo build --verbose
    - name: Run tests
      run: cargo test --verbose
